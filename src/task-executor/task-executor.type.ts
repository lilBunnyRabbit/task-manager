export enum ExecutionFlag {
  STOP = "STOP",
  FAIL_ON_ERROR = "FAIL_ON_ERROR",
}

export enum ExecutionModeFlag {
  EXECUTE_PARALLEL = "EXECUTE_PARALLEL",
  EXECUTE_SEQUENTIAL = "EXECUTE_SEQUENTIAL",
}
